version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - ls -a
      - cd reserve-my-seat-app
      - echo "Installing dependencies..."
      - npm ci --legacy-peer-deps

  build:
    commands:
      - echo "Building react app..."
      - cd reserve-my-seat-app
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: reserve-my-seat-app/dist
  discard-paths: no
